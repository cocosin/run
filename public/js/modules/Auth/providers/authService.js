!function (angular) {
    "use strict";
    angular.module('Auth').provider('auth', [
            function () {
                // auth methods must be here
            }
        ]
    );
}(angular);
